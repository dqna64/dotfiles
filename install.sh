#!/bin/bash

# This install.sh script is empty for now, not needed bc all my dotfiles
setup is currently done in `.zshrc` on zsh startup. And since this is a
bare git repo, `.zshrc` can be cloned directly into `~`.

